/**
 * Layout constants for the Materi Documents Editor.
 * Based on standard A4 dimensions at 96 DPI.
 */

export const PAGE_WIDTH = 816; // 8.5 inches * 96 DPI
export const PAGE_HEIGHT = 1056; // 11 inches * 96 DPI
export const PAGE_MARGIN = 50;

export const CONTENT_WIDTH = PAGE_WIDTH - (PAGE_MARGIN * 2);
export const CONTENT_HEIGHT = PAGE_HEIGHT - (PAGE_MARGIN * 2);

export const LINE_HEIGHT = 1.5; // Standard line height
export const FONT_SIZE = 16; // Standard font size in pixels
